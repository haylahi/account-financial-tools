<openerp>
  <data>

    <record model="ir.ui.view" id="account_move_line_manual_multicurrency">
      <field name="name">account_move_line_manual_multicurrency</field>
      <field name="model">account.move.line</field>
      <field name="inherit_id" ref="account.view_move_line_tree"/>
      <field name="arch" type="xml">
      		  <!-- First we remove field to repositionate after -->
              <field name="amount_currency" position="replace">
              </field>
      		  <!-- First we remove field to repositionate after -->
              <field name="currency_id" position="replace">
              </field>
			  <field name="debit" position="before">
                    <field name="currency_id" on_change="onchange_amount_currency(amount_currency,currency_id,date)"/>
                    <field name="amount_currency" on_change="onchange_amount_currency(amount_currency,currency_id,date)"/>
			  </field>              

      </field>
    </record>
  </data>
</openerp>